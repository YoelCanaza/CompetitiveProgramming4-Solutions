#include <bits/stdc++.h>
using namespace std;

typedef long long Long;
const char ENDL = '\n';

void solve(void) {
    int n, p; cin >> n >> p;

    cout << p << ENDL;
}

int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(0);

    solve();

    return 0;
}